<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration for Android.
  - Blocks all cleartext (HTTP) traffic in release builds.
  - Enforces HTTPS for the Supabase API domain.
  - Allows cleartext only in debug for local development.
-->
<network-security-config>
    <!-- Default: block all cleartext traffic -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Supabase API domain: HTTPS only -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">supabase.co</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Debug overrides: allow debug CA for Charles/mitmproxy during development -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
